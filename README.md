goofy recursive descent parser/lexer

To use: replace input.txt with whatever code you want to parse, run parser.py

written for whitney young's microarchitecture / logic design class with dr mauer-oats (irl [nand2tetris](https://www.nand2tetris.org))
